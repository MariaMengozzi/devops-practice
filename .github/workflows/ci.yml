name: pipeline

on:
  push:

jobs:
  my-job:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
            distribution: temurin
            java-version: 11

      - name: execute program
        run: ./gradlew run